(function (global) {
  "use strict";

  // GitHub Pages replaces this file during deployment with the exact commit
  // metadata from the workflow. The committed fallback keeps local copies
  // explicit instead of leaving the version badge blank.
  global.AppDeploymentVersion = Object.freeze({
    version: "1.6.1",
    commit: "local",
    shortCommit: "local",
    branch: "main",
    deployedAt: "",
    runId: "",
    source: "development"
  });
})(window);
